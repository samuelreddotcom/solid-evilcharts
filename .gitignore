node_modules
dist
dist-ssr
.DS_Store
*.local
*.log
.vite
coverage

# Generated by @tanstack/router-plugin
src/routeTree.gen.ts

# `shadcn build` output. Only needed if the registry is ever served over HTTP;
# under GitHub registries the repo itself is the registry and this is unused.
public/r

# NOTE: registry.json and src/registry/registry.json are generated, but they are
# COMMITTED ON PURPOSE — a GitHub registry is a repo with a root registry.json in
# it. Ignoring them makes the registry resolve to nothing, silently.
